import { ref, reactive, onMounted, onBeforeUnmount } from 'vue';
import resize from '@/utils/resize';

// export default {
//   setup() {
//     onMounted(() => {
//       const app = document.querySelector('body');
//       const transformArr = app.style.transform.split(' ');
//       app && (app.style.transform = `${transformArr[0]} rotate(0deg)`);
//     });

//     onBeforeUnmount(() => {
//       resize();
//     });
//     return {};
//   },
// };

export default {
  setup() {},
};
